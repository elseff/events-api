name: Deploy

on:
  workflow_run:
    workflows: [ Build ]
    branches:
      - 'master'
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello!!!"
